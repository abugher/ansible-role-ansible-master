---
dependencies:
  - role:       'ansible_target'
    when:       skipdeps is not defined
  - role:       'user_friendly'
    when:       skipdeps is not defined
  - role:       'ssh_client'
    when:       skipdeps is not defined
